# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

主要用户是产品作者本人：把阿里云 OSS 当作私有存储，在浏览器里管理个人照片、视频与文件（个人私有相册 / 网盘）。使用场景以个人日常归档、浏览、预览为主，非多租户或团队协作产品。

## Product Purpose

纯前端应用：选择本地文件上传至阿里云 OSS，并在线浏览与预览图片、视频、Word、PDF 等；重点能力是按拍摄日归档的相册体验。成功标准是：源文件完整上传/下载、相册按日可浏览、常见类型可预览，且无需自建业务后端即可长期自用。

## Positioning

个人自用的纯前端 OSS 客户端：以「按拍摄日归档的相册体验」为核心，配合源文件直传、无自建业务后端。相对阿里云控制台或通用网盘，差异在于相册式浏览与个人归档习惯，而非企业文件协作或运维控制台替代。

## Operating Context

- 桌面与手机浏览器响应式使用；桌面顶栏导航，移动端底部 Tab。
- 主要流程：上传 → OSS 列举/刷新 → 文件列表或相册浏览 → 弹窗或 `/preview` 深链预览。
- 凭证：优先 STS 临时凭证（本地可有受限调试方式，上线须 STS）；Bucket 宜私有 + 签名访问。
- 配套：仓库内可选 `sts-server` 签发临时凭证；配置经 `.env` / `.env.local`。

## Capabilities and Constraints

**已具备（产品事实）：**

- 多文件上传（队列、进度、取消/重试）、类型与大小限制、Object Key 规则（图片可按文件名日期 / EXIF 归档到 `yyyy/MM/dd`）。
- 文件列表（扁平/层级）、独立相册页（图片+视频、按日分组、EXIF/地点、瀑布流与虚拟滚动、多选批量下载删除）。
- 预览：图片、HTML5 视频、PDF、docx；其他类型信息展示/下载。
- 纯前端部署；无登录/权限体系（可后续扩展，当前不做）。

**硬约束：**

- 上传与下载保持源文件（不做压缩、转码、重编码）。
- 前端禁止硬编码长期 AccessKey；优先 STS。
- 不做自建文件管理业务后端/数据库；不做在线协同编辑；不做视频转码/流媒体服务端方案。
- UI 基于 Vue 3 + Element Plus + TypeScript + Vite + pnpm + `ali-oss`（既有工程，非绿场选型问题）。

**术语：** 相册（`/images`）、文件列表、上传、预览深链；OSS Object Key / 前缀目录。

## Brand Commitments

- 仓库名 / 包名：`heroxin-pic`。
- 默认展示标题：`阿鑫相册`（`VITE_APP_TITLE`，可配置）。
- 语气：简洁实用、自用工具；无对外营销品牌体系要求。
- 视觉世界（用户确认）：**天空口袋** — 亮天蓝雾底 + 信标蓝动作色 + 轻触觉；详见 `DESIGN.md`。
- README 约定主题保持简洁，避免过度装饰（实现见 `src/styles/theme.css`）。

## Evidence on Hand

- 主文档与任务记忆：`README.md`；相册二期：`docs/album-phase2.md`；OSS/STS/部署等见 `docs/`。
- 可运行的现有 SPA（上传、列表、相册、预览）及主题样式。
- 无对外客户证言、案例或营销素材；未来设计不得虚构用户评价、客户名单或商业背书。

## Product Principles

1. **自用优先**：交互与信息架构服务个人归档与浏览，不为虚构的多角色协作扩 scope。
2. **源文件为真**：上传/下载不改写媒体；相册分组尊重拍摄日/归档日事实。
3. **无业务后端**：能力边界停在浏览器 + OSS（及签发 STS 的最小辅助），不引入自建文件业务服务。
4. **桌面与手机同权**：同一套产品能力在窄屏上可完成核心任务。
5. **安全默认**：临时凭证、私有桶与签名访问优先于长期密钥与公共读捷径。

## Accessibility & Inclusion

未单独约定 WCAG 等强制标准；后续工作遵循常规 Web 可用性与既有 Element Plus 控件行为，不做超出自用范围的无障碍合规承诺。
